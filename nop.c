#include "monty.h"

/**
 * nop - doesn't make anything.
 * @stack: doubly-linked list.
 * @line_number: file's line.
 * Return: Nothing
 */

void nop(stack_t **stack, __attribute__((unused))unsigned int line_number)
{
(void)stack;
}
